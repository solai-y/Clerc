[pytest]
addopts = -q
testpaths = tests
python_files = test_*.py

markers =
    e2e: end-to-end tests (read-only; hit running service on :5007)
    integration: in-process FastAPI TestClient tests (read-only)
